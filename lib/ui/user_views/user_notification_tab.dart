import 'package:flutter/material.dart';

class UserNotificationTab extends StatelessWidget {
  const UserNotificationTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
