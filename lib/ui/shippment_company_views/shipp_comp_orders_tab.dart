import 'package:flutter/material.dart';

class ShippCompOrdersTab extends StatelessWidget {
  const ShippCompOrdersTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
