import 'package:flutter/material.dart';

class ShippCompHomeTab extends StatelessWidget {
  const ShippCompHomeTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
