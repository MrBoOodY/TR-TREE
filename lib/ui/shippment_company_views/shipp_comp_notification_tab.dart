import 'package:flutter/material.dart';

class ShippCompNotificationTab extends StatelessWidget {
  const ShippCompNotificationTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
